# Book Shop Professional v1

Python desktop starter + responsive website starter. Run `python desktop_app/app.py` with Python 3. Open `website/index.html` in a browser.

Production database, full POS, invoice printing, authentication, barcode device integration, reports, backup and voice features are planned for the next development stage.
